// переменные, в которых будет содержаться узел, который мы будет выбирать (получать по id). id взято из div в index.html
const ROOT_PRODUCTS = document.getElementById('products');
const ROOT_HEADER = document.getElementById('header');
const ROOT_SHOPPING = document.getElementById('shopping');
const ROOT_PRELOADER = document.getElementById('preloader');

